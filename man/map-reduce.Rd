% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map-reduce.R
\name{gsvaMap}
\alias{gsvaMap}
\alias{gsvaReduce}
\title{MapReduce parallelization for HPC environments}
\usage{
gsvaMap(
  paramOrGsvaExprData,
  verbose = TRUE,
  BTPARAM = BatchtoolsParam(workers = 2, progressbar = verbose)
)

gsvaReduce(..., verbose = TRUE)
}
\arguments{
\item{paramOrGsvaExprData}{An object either of class \code{\link{gsvaParam}}, or
obtained with \code{\link{gsvaRowNorm}} or \code{\link{gsvaColRanks}}, in this context run through
\code{gsvaMap()}. In the latter case, it must be one of the classes supported by
\code{\link[=GsvaExprData-class]{GsvaExprData}}.  For a list of these classes, see its help page
using \code{help(GsvaExprData)}.}

\item{verbose}{Gives information about the progress of the calculations.
Default: \code{TRUE}.}

\item{BTPARAM}{An object of class
\code{\link[BiocParallel:BatchtoolsParam-class]{BatchtoolsParam}} specifying parameters
for parallel execution in an HPC enviroment.}

\item{...}{In \code{gsvaReduce()}, the output of \code{gsvaMap()}.}
}
\value{
The \code{gsvaMap()} function returns either a list of objects with the
results of the GSVA calculations for each compute node, or a list of file paths
where the results are saved. The \code{gsvaReduce()} function returns a single
object that combines the results from all compute nodes.
}
\description{
The functions \code{gsvaMap()} and \code{gsvaReduce()} allow one
to run GSVA calculations across multiple compute nodes in a high-performance
computing (HPC) environment. The \code{gsvaMap()} function is used to launch the
GSVA calculations on each compute node, while the \code{gsvaReduce()} function is
used to combine the results from all nodes into a single object.
}
\examples{

p <- 10 ## number of genes
n <- 30 ## number of samples
nGrp1 <- 15 ## number of samples in group 1
nGrp2 <- n - nGrp1 ## number of samples in group 2

## consider three disjoint gene sets
geneSets <- list(gset1=paste0("g", 1:3),
                 gset2=paste0("g", 4:6),
                 gset3=paste0("g", 7:10))

## sample data from a normal distribution with mean 0 and st.dev. 1
y <- matrix(rnorm(n*p), nrow=p, ncol=n,
            dimnames=list(paste("g", 1:p, sep="") , paste("s", 1:n, sep="")))

## build GSVA parameter object
gsvapar <- gsvaParam(y, geneSets)

## calculate row-normalized expression values in parallel across multiple
## compute nodes in a high-performance computing (HPC) environment
gsvarownorm <- gsvaReduce(gsvaMap(gsvapar))

}
