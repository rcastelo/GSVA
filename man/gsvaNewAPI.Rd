% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsvaNewAPI.R
\name{gsva,plageParam,missing-method}
\alias{gsva,plageParam,missing-method}
\alias{gsva,zscoreParam,missing-method}
\alias{gsva,ssgseaParam,missing-method}
\alias{gsva,gsvaParam,missing-method}
\title{Gene Set Variation Analysis}
\usage{
\S4method{gsva}{plageParam,missing}(
  expr,
  gset.idx.list,
  annotation,
  min.sz = 1,
  max.sz = Inf,
  parallel.sz = 1L,
  verbose = TRUE,
  BPPARAM = SerialParam(progressbar = verbose)
)

\S4method{gsva}{zscoreParam,missing}(
  expr,
  gset.idx.list,
  annotation,
  min.sz = 1,
  max.sz = Inf,
  parallel.sz = 1L,
  verbose = TRUE,
  BPPARAM = SerialParam(progressbar = verbose)
)

\S4method{gsva}{ssgseaParam,missing}(
  expr,
  gset.idx.list,
  param,
  annotation,
  min.sz = 1,
  max.sz = Inf,
  parallel.sz = 1L,
  verbose = TRUE,
  BPPARAM = SerialParam(progressbar = verbose)
)

\S4method{gsva}{gsvaParam,missing}(
  expr,
  gset.idx.list,
  annotation,
  min.sz = 1,
  max.sz = Inf,
  parallel.sz = 1L,
  verbose = TRUE,
  BPPARAM = SerialParam(progressbar = verbose)
)
}
\arguments{
\item{expr}{Gene expression data which can be given either as a
\code{\link{SummarizedExperiment}}, \code{\link{SingleCellExperiment}}
\code{\link{ExpressionSet}} object, or as a matrix of expression
values where rows correspond to genes and columns correspond to samples.
This matrix can be also in a sparse format, as a \code{\linkS4class{dgCMatrix}}, or
as an on-disk backend representation, such as \code{\link{HDF5Array}} .}

\item{gset.idx.list}{Gene sets provided either as a \code{list} object or as a
\code{\link{GeneSetCollection}} object.}

\item{annotation}{In the case of calling \code{gsva()} on a
\code{\link{SummarizedExperiment}} or \code{\link{SingleCellExperiment}} object,
the \code{annotation} argument can be used to select the assay
containing the molecular data we want as input to the \code{gsva()}
function, otherwise the first assay is selected.
In the case of calling \code{gsva()} with expression data in
a \code{matrix} and gene sets as a \code{\link{GeneSetCollection}}
object, the \code{annotation} argument can be used to supply
the name of the Bioconductor package that contains
annotations for the class of gene identifiers occurring in
the row names of the expression data matrix.
In the case of calling \code{gsva()} on a
\code{\link{ExpressionSet}} object, the \code{annotation} argument
is ignored. See details information below.}

\item{min.sz}{Minimum size of the resulting gene sets.}

\item{max.sz}{Maximum size of the resulting gene sets.}

\item{parallel.sz}{Number of threads of execution to use when doing the calculations in parallel.
The argument BPPARAM allows one to set the parallel back-end and fine
tune its configuration.}

\item{verbose}{Gives information about each calculation step. Default: \code{FALSE}.}

\item{BPPARAM}{An object of class \code{\link{BiocParallelParam}} specifiying parameters
related to the parallel execution of some of the tasks and calculations within this function.}

\item{param}{A parameter object determining the analysis method to be performed
as well as containing any method-specific parameters.}
}
\value{
A gene-set by sample matrix (of \code{matrix} or \code{\linkS4class{dgCMatrix}} type,
depending on the input) of GSVA enrichment scores.
}
\description{
Run PLAGE on a matrix with gene sets in a list.

Run z-score analysis on a matrix with gene sets in a list.

Run ssGSEA on a matrix with gene sets in a list.

Run GSVA on a matrix with gene sets in a list.
}
\section{Functions}{
\itemize{
\item \code{gsva(expr = plageParam, gset.idx.list = missing)}: Run PLAGE on a matrix with gene sets in a list.

\item \code{gsva(expr = zscoreParam, gset.idx.list = missing)}: Run z-score analysis on a matrix with gene sets in a list.

\item \code{gsva(expr = ssgseaParam, gset.idx.list = missing)}: Run ssGSEA on a matrix with gene sets in a list.

\item \code{gsva(expr = gsvaParam, gset.idx.list = missing)}: Run GSVA on a matrix with gene sets in a list.

}}
\seealso{
\code{\link{plageParam}}, \code{\link{zscoreParam}}, \code{\link{ssgseaParam}}, \code{\link{gsvaParam}}
}
